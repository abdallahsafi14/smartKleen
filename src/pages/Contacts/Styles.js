import styled from "styled-components";

export const Styles = styled.div`
  .contacts-wrapper {
    overflow: hidden;
    max-width: 100%;
    padding: 2rem;
    margin: 0 auto;
  }
  
  .contacts-header h2{

    font-size: 30px;
  }
  .contacts-header {
    text-align: center;
    font-family: "GOTHIC";
  }
  .contacts-content {
  text-align: center;
  margin-top: 2rem;
}

`;
